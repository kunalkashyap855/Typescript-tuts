// variable type annotation
const first_name: string = "Kunal";

// paramter type annotation
function greet(name: string) {
    console.log(`Hello, ${name}!`)
}

// return type annotation
function getFavoriteNumber(): number {
    return 7;
}